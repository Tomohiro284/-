/*!
 * site-config.js — サイト全体の設定
 *
 * ★公開前に、このファイルの値を自分のものに書き換えてください。
 *   ここを直すだけで、全ページの広告・連絡先が切り替わります。
 */
window.SITE_CONFIG = {
  // 公開URL（末尾のスラッシュまで含める）
  // GitHub Pages の既定URL。独自ドメインを使う場合はそのURLに変更する。
  url: 'https://tomohiro284.github.io/-/',

  // 運営者情報（about.html / contact.html に表示される）
  owner: '（運営者名をここに）',
  email: '（連絡用メールアドレスをここに）',

  /* ------------------------------------------------------------------ *
   * 広告（Google AdSense）
   *
   * client を空のままにしておくと広告枠は表示されません。
   * 審査に通ってから、発行された ID を入れてください。
   *
   *   client … 「ca-pub-」で始まるパブリッシャーID
   *   slots  … 広告ユニットごとの「広告スロットID」（数字10桁）
   *
   * AdSense の管理画面で「ディスプレイ広告」を2つ作り、
   * それぞれのスロットIDを content / footer に貼ります。
   * ------------------------------------------------------------------ */
  adsense: {
    client: '', // 例: 'ca-pub-1234567890123456'
    slots: {
      content: '', // 記事の途中に出す広告
      footer: '',  // ページ下部に出す広告
    },
  },
};
